# Not required for the default setup (Vite dev proxy / nginx reverse proxy
# both put the frontend and API on the same origin at the relative path
# /api). Only set this if the API is genuinely served from a different
# origin than the frontend.
# VITE_API_BASE_URL=https://api.example.com/api
